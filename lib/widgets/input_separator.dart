import 'package:flutter/material.dart';

class InputSeparator extends StatelessWidget {
  const InputSeparator({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 18,
    );
  }
}
